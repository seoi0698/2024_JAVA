다음 항목을

'나이', '생일(mm.dd)', '이름', '전공' 

기본 타입 변수와 참조 타입 변수를 활용하여 

각 항목이 어떤 타입 또는 클래스로 선언되었는지 서술하고

ex) 

int age = 20;

String name = "홍길동";

영역과 특징이 드러날 수 있도록 그림으로 표현하시오.

//정답
나이 -> int type(기본타입 변수)
생일, 이름, 전공 -> str type(참조타입 변수)

